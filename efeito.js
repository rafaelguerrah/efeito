function parar(){
    const para = document.querySelectorAll(".lt").forEach(function(el){
        el.style.animation = "rotate 15s linear infinite";
       
        
    })

}

function iniciar(){
    const iniciar = document.querySelectorAll(".lt").forEach(function(el){
        el.style.animation ="rotate 8s linear infinite"
         
    })
        
    }
